export class Toast {
    constructor(root, id, img, title, text, alt) {
        this.root = root
        this.id = id
        this.text = text
    }

    render() {
        this.root.innerHTML = `
            
        `;
    }
}

